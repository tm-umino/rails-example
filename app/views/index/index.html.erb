<nav class="navbar navbar-default">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbarEexample6">
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
  </div>
  <div class="collapse navbar-collapse" id="navbarEexample6">
    <ul class="nav navbar-nav">
      <li><%= link_to 'News', news_index_path %></li>
      <li><%= link_to 'User', users_path %></li>
    </ul>
    <% if @current_user %>
      <p class="navbar-text navbar-right"><%= @current_user.name %> | <%= link_to 'ログアウト', logout_path, method: :delete %></p>
    <% else %>
      <p class="navbar-text navbar-right"><%= link_to 'ログイン', login_path %></p>
    <% end %>
  </div>
</nav>

<table class="table">
  <thead>
    <tr>
      <th>Title</th>
      <th>Writer</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @news.each do |news| %>
      <tr>
        <td><%= news.title %></td>
        <td><%= news.writer %></td>
        <td><%= link_to 'Show', news %></td>
      </tr>
    <% end %>
  </tbody>
</table>
